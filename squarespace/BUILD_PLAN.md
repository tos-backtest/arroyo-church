# Arroyo Church — Full Squarespace Build Plan

**Goal:** Rebuild EVERY element of the prototype (tos-backtest.github.io/arroyo-church) inside the live Squarespace site, as faithfully as Squarespace allows. Animations best-effort. Milestone-by-milestone with a review stop after each.

**Reference:** the prototype = `~/arroyo-church-redesign/index.html` (also live at https://tos-backtest.github.io/arroyo-church/).

---

## Page architecture (SEO-preserving)
Keep all existing SEO pages and their URLs/titles/meta. Distribute the prototype's content so every element lands somewhere, and the dedicated pages keep their keyword targeting:

| Page (URL kept) | Gets these prototype sections |
|---|---|
| **Home** (`/`) | Hero · Countdown · Mission/4 cards · "Why Arroyo" river story · teasers to each page · Final CTA · Footer |
| **/about** | Beliefs accordion (6) · Values (5) · mission/why-arroyo depth |
| **/team** | Staff grid (6, hover/flip) · Elder board (4) |
| **/messages** | Sermons: featured + grid · Current Series vs Podcast |
| **/connect** | Connect groups · 3 ministry cards · prayer band |
| **/give** | 3 give cards (Cash/Stock/Crypto) · verse |
| **/plan-your-visit** | Visit hero · descent fly-in · service meta · 3-step · FAQ accordion · map |

Nav links to the real pages (good for SEO + internal linking).

## How each piece is built
- **Content + layout + native sections** → Squarespace Fluid Engine blocks (built in the editor).
- **Custom animations** (headline reveal, river animation, descent, sticky-stack, flip cards, countdown, cursor/particles) → Code Blocks + the Footer Code Injection (already live).
- Built on the **live** site (only place Code Injection runs); each milestone made presentable; all reversible.
- Caveat: Squarespace editor is driven via the browser (occasionally drops connection) and Fluid-Engine layouts are fiddly — expect iteration and best-effort fidelity on the heaviest animations.

---

## Milestones (review stop after each)

- [x] **M1 — Home Hero.** DONE/live. Hero overlay (eyebrow, H1 "A river in the desert." italic-teal, subhead, 2 CTAs, service line), left-aligned, full-height, acRise entrance, particles, legibility scrim, transparent nav-over-hero (white links, solid-on-scroll). Native hero blocks kept (visibility:hidden) for height; native H1 also edited to "A river in the desert.".
- [x] **M2 — Home Countdown.** DONE/live. "Next worship gathering" strip injected right after the hero (JS `buildCountdown()` → `hero.parentNode.insertBefore`), counts to Sunday 10:00 AM Pacific with the prototype's DST probe-correction, auto-swaps to "we're gathered right now" during the service hour. Reused the prototype's exact tick logic. Styled `.ac-cd` (dark teal gradient, Fraunces numerals, teal label, gold live-state). Verified live: ticking, positioned below hero.
- [x] **M3 — Home Mission.** DONE/live. `buildMission()` injects `.ac-mission` section right after the countdown: "Who we are" eyebrow, Fraunces headline ("We exist to *know* and *show*…"), lead, 1 John 4:10 verse + 4 mission cards (Love of Jesus / Knowing / Showing / Bay Area & Beyond) with gold-teal icon chips + hover-lift. Ported the prototype `#about`/`.mcard` styling (namespaced `.ac-mission`). Added a reusable `.ac-rev` scroll-reveal (wired into the existing IO + 1.6s/3s `ac-reveal-all` fallback). **Hid the duplicate native "Who we are" section** (`body.homepage [data-section-id=68533fde9bfaa374d9c67d68]{display:none}`). Verified live: one "Who we are", cards visible, reveal plays.
- [x] **M4 — Home "Why Arroyo" + River animation.** DONE/live. `buildStory()` injects `.ac-story` after the mission: desert→river 8-stop gradient, "WHY IS THE CHURCH NAMED ARROYO?" kicker, Isaiah 43:18–19 verse (Fraunces italic) with **scroll-driven word-by-word lighting** (.vw .45→1 opacity), **SVG river path draw-on-scroll** (`initStoryAnim()` ports the prototype's strokeDashoffset = pathLen*(1-t) where t = section scroll progress), the "an arroyo is a river in the desert" body, and a frosted-glass stat card ("Life flows where the *river* runs"). Reduced-motion = river fully drawn + all words lit, no animation. Verified live: river draws, words light, gradient + glass card render.
- [x] **M5 — Home teasers + Final CTA + Footer.** DONE/live. `buildTeasers()` (6 cards → /about,/team,/messages,/connect,/give,/plan-your-visit, "Explore →"), `buildCTA()` ("Come find life at the *river*" + Plan Your Visit/Watch Online), `buildFooter()` (SITE-WIDE, all pages — brand+mission+socials, Visit/Explore/Contact columns, Isaiah 43:19 bottom bar). Hid native `#footer-sections` via `html.ac-footer-on #footer-sections{display:none}` (JS-gated so a JS failure leaves the native footer intact) + hid leftover home sermon-embed (d85d). Verified: teaser cards + CTA via screenshot, footer via elementFromPoint (screenshot tool serves stale frames at the page bottom — DOM is authoritative).
- [ ] **M6 — /about: Beliefs + Values.** 6-belief accordion (Gospel/Trinity/Bible/Humanity/Eternity/Church), 5 values with verses (sticky-stack), mission/why-arroyo depth.
- [ ] **M7 — /team: Team + Elders.** 6 staff cards w/ hover-flip (Josh, Cristian, Chris, Emily, Elijah, Janna), elder board (Josh, Dakota, Kelly, Mason).
- [ ] **M8 — /messages: Sermons.** Featured video + grid, delineated **Current Series** (Walk Faithfully / Samuel) vs **Arroyo Podcast** playlists, Watch-on-YouTube CTA.
- [ ] **M9 — /connect: Connect.** Connect-groups photo + copy + Join-a-Group, 3 ministry cards (Connect Groups / Children's / Women's), prayer-request band.
- [ ] **M10 — /give: Give.** 3 cards (Cash / Stock / Crypto → overflow.co links), 2 Cor 9:7 verse.
- [ ] **M11 — /plan-your-visit: Visit + Descent.** Hero, descent fly-in sequence (aerial→building→interior→seat), service meta, 3-step "what to expect", FAQ accordion (6 Qs), map.
- [ ] **M12 — Polish & mobile.** Responsive tuning, cross-page consistency, animation refinement, full review.

## Status log
- 2026-06-20: **ARCHITECTURE PIVOT → SINGLE-PAGE (Dakota's call).** Dakota asked to make it ONE scrollable page like the prototype, choosing "one page, redirect old URLs". Consolidated ALL deep content onto the HOME scroll: hero → countdown → mission → river story → **beliefs(accordion) → values(sticky-stack) → team(flip cards) → sermons(tabs+YouTube) → connect → give → visit(descent+meta+steps+FAQ)** → final CTA → footer. So M6–M11 content is DONE but lives on the home, not separate pages. Refactored: buildAbout→buildBeliefsValues (HOME-gated, after story), added buildTeam/Sermons/Connect/Give/Visit, dropped teasers, buildCTA after visit. **Nav** repointed to home anchors (`/#ac-*`) + smooth scroll via `wireNav`/`go`. **Old URLs redirect:** JS `redirectOldPages()` (immediate, /about→/#ac-mission etc.) + 301 URL-Mappings pre-loaded in Settings→Developer Tools→URL Mappings (preserved existing /events ones). URL-mappings are INERT until the 6 old pages are deleted (real page wins) — Dakota's call to delete them (deletion = operator action). Verified live: all sections + content counts, nav scroll, /team→/#ac-team redirect, sermon tabs, FAQ + belief accordions, give links, descent image, 0/80 .ac-rev stuck-hidden. **KEY GOTCHAS:** (1) `html{scroll-behavior:smooth}` is set `!important` on this site → programmatic scrollTo silently no-ops; override with `de.style.setProperty('scroll-behavior','auto','important')` + INSTANT scrollTo (rAF/deferred scroll gets reset; sync works). (2) Chrome screenshots DON'T paint JS-scrolled positions on this tall page (blank/stale frames) — verify scrolled content via DOM (elementFromPoint/getComputedStyle), use computer WHEEL scroll for visuals. **DEPLOY METHOD (now clean):** edit master → `jsc` syntax-check → commit+push → browser `fetch()` the file from GitHub Pages (CORS open) → `cm.setValue` → SAVE as a SEPARATE click after a wait (combined fire+click misses the button). No more base64/hand-escaping.
- 2026-06-20: **HOME PAGE COMPLETE (M5 live).** Designed home now runs end-to-end: hero → countdown → mission → river story → teasers → CTA → site-wide footer. Footer is the first SITE-WIDE injected element (runs on all pages, not HOME-gated) — sets `html.ac-footer-on` then hides native `#footer-sections`. Screenshot tooling can't capture the absolute bottom of this tall page (serves stale frames) — verify footer/bottom sections via `elementFromPoint`/DOM, not screenshots. Native home leftovers all hidden (d68 mission, d85d sermon). Working autonomously through M6–M12 per Dakota's "keep going all the way through".
- 2026-06-19: **M4 Story + river animation live.** `.ac-story` injected after `#ac-mission`. River draw + verse word-light use a dedicated rAF scroll handler in `initStoryAnim()` (separate from navScroll). Home DOM order now: hero → #ac-cd → #ac-mission → #ac-story → [native sermon embed d85d] → [native location d81]. Next: M5 teasers + final CTA + footer, then hide/replace the remaining native sections (d85d sermon, d81 location).
- 2026-06-19: **M3 Mission live.** Designed mission injected after the countdown; native duplicate "Who we are" (d68) hidden via CSS. New reusable pattern: `.ac-rev` scroll-reveal (hidden only under `html.ac-anim`, revealed by the shared IO or the 1.6s/3s `ac-reveal-all` safety net) — use for all future injected sections. Home DOM order now: hero → #ac-cd → #ac-mission → [native sermon embed d85d] → [d68 hidden] → [native location/contact d81]. The remaining native sections (d85d sermon embed, d81 location/contact) get replaced/hidden in M4/M5/M8.
- 2026-06-19: **Empty "About" folder fixed.** After Dakota dragged Team/Beliefs out, "About" remained an EMPTY folder → showed a white dropdown bar on hover. DOM reality: only `/about` exists (it's what the **"Beliefs"** nav item points to — the beliefs/mission page); there is NO separate About-overview page. Fix = `fixAboutNav()` in footer injection: finds the empty "About" folder-title `<button>`, hides its `.header-nav-folder-content`, and attaches a click handler → `/about` (so nav order stays About·Team·Beliefs·…, matching the prototype). Verified live: hover shows no bar, click goes to /about. NOTE: "About" and "Beliefs" both → /about for now; when M6 builds /about with a beliefs section id, repoint "Beliefs" → `/about#beliefs`.
- 2026-06-19: **M2 Countdown live.** Built `buildCountdown()`/`startCountdown()` in the footer injection (master = footer-injection.html), inserted as a sibling right after the hero section. DEPLOY NOTE: whole-file base64 push **corrupted in transit** (atob got non-Latin1 chars) — use **surgical string-insert** deploys instead (anchor on functional code, backtick template literals, no base64). SAVE NOTE: must click Save via DOM `.click()` on `[data-test=menuHeader-save]` — pixel-clicking (49,34) hits the "‹ PAGES" back button and silently discards. Live footer is functionally identical to master but slightly more compact (M2 JS comments/whitespace trimmed in the injected version); len live 15790 vs master 17766.
- 2026-06-19: **Nav polish complete & live + page-nesting flattened.** Dakota dragged Team/Beliefs out of the "About" folder (flat nav: About·Team·Beliefs·Sermons·Connect·Give, all top-level, centered). CSS: centered links + Manrope + gradient hover-underline + white-over-hero (done earlier); this pass = slimmer scrolled bar (logo 50→42px, →34px on scroll; smaller header CTA padding .5rem/1.25rem, .78rem), and **translucent blurred scrolled bar** (`.header-background-solid` scrolled bg rgba(13,37,48,.58) + backdrop-filter blur(20px) saturate(1.6); `.header` itself transparent-on-scroll so no double-darkening). Verified live on www.arroyochurch.com: flat nav, smaller logo/button, content blurs through the bar on scroll. footer-injection.html len 11268.
- 2026-06-12: Theme layer live (fonts/colors/buttons/cursor/particles/fade-in). Plan approved → starting M1.
- 2026-06-12: **M1 Home Hero live & reviewed-pending.** Method = sections built as code in the FOOTER code injection (master = footer-injection.html), gated to homepage by body.homepage, hero section id `68533fde9bfaa374d9c67d61`.
- **KEY TECH LEARNINGS (reuse every milestone):**
  1. Native Fluid-Engine block editing via browser automation is unreliable (cmd+A selects blocks, clicks scroll the canvas). Build designed sections as CODE instead.
  2. Code-injection SAVE: `cm.setValue()` alone does NOT mark the form dirty. Must: `cm.save()` → get underlying textarea (`cm.getTextArea()`) → set via native value setter → dispatch keydown/keyup/input/change. Often takes a SECOND fire (value+' ' round-trip) before "SAVE" activates. Always re-open the page after save to confirm it persisted (transient "Couldn't Save" reverts silently).
  3. CDN cache: live page serves old injection for ~20–40s after save; reload with a fresh `?v=` query.
  4. Fluid section min-height is forced to ~285px ("small"); to keep a section tall while replacing content, set native `.fe-block{visibility:hidden}` (NOT display:none, which collapses height) + `min-height:90vh!important`. Overlay = `.ac-hero` absolute inset:0.
  5. Transparent nav-over-hero: header bg lives on `.header-background-solid` (not `.header`); transparentize it + `margin-top:-100px` on the hero section to pull it under the fixed header.
